# 主な代入演算子
    # =(変数の値を代入)
    # += (左辺と右辺を足した結果を左辺に代入)
    # -= (左辺から右辺を減算した結果を左辺に代入)
    # *= (左辺と右辺を乗算した結果を左辺に代入)
    # /= (左辺を右辺で除算した結果を左辺に代入)
    # //= (左辺を右辺で除算した結果の整数部分を左辺に代入)
    # %= (左辺を右辺で乗算した余りを左辺に代入)
    # **=(左辺を右辺でべき乗した結果を左辺に代入)
    # &= (左辺と右辺をビット論理積をした結果を左辺に代入)
    # ^= (左辺と右辺をビット排他論理和した結果を左辺に代入)
    # |= (左辺と右辺をビット論理和した結果を左辺に代入)
    # >>= (左辺を右辺の値だけ右シフトした結果を左辺に代入)
    # <<= (左辺を右辺の値だけ左シフトした結果を左辺に代入)
